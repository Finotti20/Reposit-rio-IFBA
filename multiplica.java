void main(){

    int n1 = Integer.parseInt(IO.readln("Digite o primeiro número: "));
    int n2 = Integer.parseInt(IO.readln("Digite o segundo número: "));

    int multi = n1 * n2;

    IO.println("O resultado da multiplicação é: " + multi);
}